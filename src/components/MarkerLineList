import React from "react";
import MarkerLine from "./MarkerLine";
import { v4 as uuidv4 } from "uuid";

export default function MarkerList({ lines }) {
  return lines.map((line) => {
    return <MarkerLine key={line.key} line={line} />;
  });
}
